Run:

```powershell
dotnet run -- "C:\Users\admin\Pictures\xxx.png"
```

Or interactive:

```powershell
dotnet run
```

Config: edit `config.json`.

Note: If `enable_webp=true`, this tool calls external `cwebp`.
