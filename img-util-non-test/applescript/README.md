# AppleScript version (curl driven)

This AppleScript implements the same flow by calling `curl`, `md5`, and optionally `cwebp`.

It is intended for macOS.

Run:

```bash
osascript main.applescript "/path/to/image.png"
```

Config is at the top of the script.
